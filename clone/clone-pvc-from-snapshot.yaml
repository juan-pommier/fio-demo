apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fio-clone-pvc
spec:
  storageClassName: vmstore-csi-file-driver-sc
  dataSource:
    name: snapshot-fio-pvc
    kind: VolumeSnapshot
    apiGroup: snapshot.storage.k8s.io
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
