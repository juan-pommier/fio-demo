apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-demo-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1.5Gi
  storageClassName: vmstore-csi-file-driver-sc
